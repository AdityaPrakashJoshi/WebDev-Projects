<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User List</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-black text-white flex items-center justify-center min-h-screen">
    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold mb-4 text-center">User List</h1>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% users.forEach(function(user) { %>
                <div class="bg-gray-800 rounded-lg shadow-lg p-6 max-w-sm mx-auto">
                    <p class="bg-zinc-500 rounded-lg p-2 mb-2 text-white"><strong>ID:</strong> <%= user.id %></p>
                    <p class="bg-zinc-500 rounded-lg p-2 mb-2 text-white"><strong>First Name:</strong> <%= user.first_name %></p>
                    <p class="bg-zinc-500 rounded-lg p-2 mb-2 text-white"><strong>Last Name:</strong> <%= user.last_name %></p>
                    <p class="bg-zinc-500 rounded-lg p-2 mb-2 text-white"><strong>Email:</strong> <%= user.email %></p>
                    <p class="bg-zinc-500 rounded-lg p-2 mb-2 text-white"><strong>Gender:</strong> <%= user.gender %></p>
                    <p class="bg-zinc-500 rounded-lg p-2 mb-2 text-white"><strong>IP Address:</strong> <%= user.ip_address %></p>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>
