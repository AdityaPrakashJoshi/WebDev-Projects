<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete User</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-black text-white flex items-center justify-center min-h-screen">
    <div class="container mx-auto p-4">
        <h1 class="text-3xl font-bold mb-4 text-center">Delete User</h1>
        <% if (errorMessage) { %>
            <p class="text-red-500"><%= errorMessage %></p>
        <% } %>
        <form action="/delete-user" method="POST" class="bg-gray-800 rounded-lg shadow-lg p-6 max-w-sm mx-auto">
            <div class="mb-4">
                <label for="id" class="block text-sm font-medium">User ID:</label>
                <input type="text" id="id" name="id" class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm" required>
            </div>
            <button type="submit" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">Delete User</button>
        </form>
    </div>
</body>
</html>
